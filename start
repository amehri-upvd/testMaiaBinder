#!/bin/bash
# Set environment variables for parallel HDF5/h5py
export CC=mpicc
export HDF5_MPI="ON"

# Source MAIA environment if available
if [ -f "/home/jovyan/Maia/build/source.sh" ]; then
  source /home/jovyan/Maia/build/source.sh
fi

# Sed to replace /home/jcoulet per /home/jovyan in notebooks
find . -type f -name "*.ipynb" -exec sed -i 's|/home/jcoulet|/home/jovyan|g' {} +

# Create Directory cible
mkdir -p Public/maia_training/MESHES
mv *.gz Public/maia_training/MESHES/
cd Public/maia_training/MESHES/
gzip -d *.gz
if [ -f "airplane_eltR4_fields.cgns" ] && \
   [ -f "airplane_eltR4.cgns" ] && \
   [ -f "rotor37_small.cgns" ]; then
  cd /home/jovyan
fi
#voila/render/tuto_maia.ipynb
#voila/render/tuto_maia_check_outputs.ipynb
#voila/render/compute_centers.ipynb

# Launch voila
#voila compute_centers.ipynb 

voila test_notebook_with_voila.ipynb 



exec "$@"
